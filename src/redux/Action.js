export const navShow=(payload)=>({
    type:"SHOW",
    data:payload

})
export const breadcrumb=(payload)=>({
    type:"BREAD_CRUMB",
    data:payload

})
export const speciality=(payload)=>({
    type:"SPECIALITY",
    data:payload

})
export const ProviderFromObj=(payload)=>({
    type:"Privider_Form",
    data:payload
})